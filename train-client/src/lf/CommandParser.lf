target python {
  keepalive: true,
  files: ["src/utils/app_logger.py"]
}

reactor CommandParser {
    preamble {=
        from app_logger import logger
    =}
  input command;
  output motor_control;
}