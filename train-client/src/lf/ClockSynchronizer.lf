target python {
  keepalive: true,
  files: ["src/app_logger.py"]
}

reactor ClockSynchronizer {
    preamble {=
        from app_logger import logger
    =}
    output clock_sync_train;
    output clock_sync_rc;
}
