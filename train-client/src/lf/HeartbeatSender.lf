target Python {
  keepalive: true,
  files: ["src/app_logger.py"]
}

reactor HeartbeatSender {
    preamble {=
        from app_logger import logger
    =}
  output heartbeat;
  timer t(1s)

  reaction(t) -> heartbeat {=

  =}
}