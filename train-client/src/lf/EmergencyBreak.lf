target python {
  keepalive: true,
  files: ["src/app_logger.py"]
}

reactor EmergencyBreak {
    preamble {=
        from app_logger import logger
    =}
    input heartbeat_rc;
    output motor_control;
}

