target python {
  keepalive: true,
  files: ["src/app_logger.py"]
}

reactor Parser {
    preamble {=
        from app_logger import logger
    =}
	input data;

	output command;
	output clock_sync;
	output heartbeat;
}