target Python {
  	keepalive: true,
  		files: ["src/app_logger.py"]
}

reactor Motor {
    preamble {=
        from app_logger import logger
    =}
  	input[5] motor_control;
    output current_speed;

}