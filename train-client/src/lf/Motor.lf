target Python {
  	keepalive: true,
  		files: ["src/app_logger.py"]
}

reactor Motor {
    preamble {=
        from app_logger import logger
    =}
  	input target_speed;
    input direction;
    input start;
    input[2] stop;

    output current_speed;

}