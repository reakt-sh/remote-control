target Python {
  keepalive: true,
  files: ["src/utils/app_logger.py"]
}

reactor LatencyCalculator {
    preamble {=
        from app_logger import logger
    =}
  input command;
  output latency;
}
